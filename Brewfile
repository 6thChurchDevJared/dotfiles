# Shared across both machines. `brew bundle --file=Brewfile`
tap "homebrew/bundle"

# Rust / oxide CLI replacements
brew "bat"        # cat
brew "eza"        # ls
brew "fd"         # find
brew "ripgrep"    # grep
brew "zoxide"     # cd
brew "git-delta"  # diff pager (installed, not yet wired into gitconfig)

# Shell + terminal
brew "fzf"
brew "zellij"
brew "wget"

# Editors / git
brew "neovim"
brew "lazygit"
brew "gh"

# Local models
brew "ollama"
